/**
 * PremiumModal — DISABLED in v1.0
 *
 * This component is intentionally a no-op stub.
 * NearBudy v1.0 focuses on user growth. All features are free.
 *
 * To re-enable Premium in a future version:
 *   1. Restore the full component from git history (see PremiumModal.css for styles).
 *   2. Restore isUserPremium in utils/premium.js.
 *   3. Re-add billing logic and premium gates in AppContext, Profile, Home, etc.
 */

// eslint-disable-next-line no-unused-vars
const PremiumModal = (_props) => null;

export default PremiumModal;
